extends layout

block content
    h1 You have removed #{firstname} #{lastname} from the database. You monster.
    p #{firstname} had the Buff ID of #{buffID}.
    form(action="/test")
        input(type="submit", value="Go to Test")